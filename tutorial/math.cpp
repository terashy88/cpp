#include <cmath> // allow math functions
#include <iostream>

using namespace std;

int main() {
  double a = 6, b = 2;
  double sum = 0;
  double plus = 0;
  double minus = 0;
  double devind = 0;
  double multibl = 0;

  plus = a + b;
  cout << plus << endl;

  // todo
  minus = a - b << endl;
  cout << minus << endl;

  double y = pow(10, 2) * 2;
  cout << y << endl;

  devind = a / b;
  cout << devind;

  multibl = a * b;
  cout << multibl;
  int c = 0, d = 0;
  // int modulus = c % d; // modulus
  // cout << modulus << endl;

  // sum= a ^ b;

  cout << sum;

  // increment
  // a = a++;
  // a = ++b;
  // a = a + 11;
  // cout << a << endl;
  // cout << b << endl;

  // multification first:
  // double x 1+2*3;
  // cout << x;

  // cout << a + b << endl;
  // cout << a - b << endl;
  // cout << a / b << endl;
  // cout << a * b << endl;
  // cout << a ^b<< endl;

  // cin.get();
  return 0;
}
